package music

import org.scalatest.FunSuite

/**
 * Test for Time.scala
 */
class TimeTest extends FunSuite {
  /*
  test("Scale pulseparts") {
    val pulseParts = PulseParts(Seq(1, 2, 5))
    assert(pulseParts.scale(13) === Seq(1.625, 3.25, 8.125))
  }

  test("Scale and sum pulseparts") {
    val pulseParts = PulseParts(Seq(1, 2, 5))
    assert(pulseParts.scaleAndSum(2, 13) === Seq((2, 1.625), (3.625, 3.25), (6.875, 8.125)))
  }
  */
}
